## Setup

### Requirements
*Ruby ruby 2.6.6
*Bundler 2.3.14

Setup Steps
* Clone repo `git clone <link to repo>`
* Run `bundle install`·
* Setup database `rails db:setup`
* Setup Admin `rails db:seed`
* Run server `rails s`

Test Case Steps:
* Run rspec spec/models
